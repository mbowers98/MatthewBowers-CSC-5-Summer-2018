/* 
 * File:   main.cpp
 * Author: Dr. Mark E. Lehr
 * Created on May 29th, 2018, 11:20 AM
 * Purpose:  CPP Template
 *           To be copied for each project
 */

//System Libraries
#include <iostream> //I/O Library -> cout,endl
#include <iomanip>  //Format Library
using namespace std;//namespace I/O stream library created

//User Libraries

//Global Constants
//Math, Physics, Science, Conversions, 2-D Array Columns

//Function Prototypes

//Execution Begins Here!
int main(int argc, char** argv) {
    //Declare Variables

    //Initial Variables
    string Month1,Month2,Month3;
    double Rain1,Rain2,Rain3;
   
    
    cout<< "Name the first month a how much rainfall it had" << endl;
            cin >> Month1 >> Rain1;
    cout<< "Name the second month and how much rainfall it had" << endl;
    cin >> Month2 >> Rain2;
    cout << "Name the third month and how much rainfall it had" << endl;
    cin >> Month3 >> Rain3;
    double Total = (Rain1 + Rain2 + Rain3)/3; 
    cout << "The average rainfall for " << Month1 << "," << Month2 << ",and " << Month3 << " is " << Total << " inches."  ;
            
    //Map/Process Inputs to Outputs

    //Exit program!
    return 0;
}